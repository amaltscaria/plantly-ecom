pipeline:
  agent:
    any
  stages:
    - stage:
        name: 'Build'
        steps:
          - script:
              - npm install
              - npm test
    - stage:
        name: 'Deploy'
        steps:
          - script:
              - npm run build
              - echo 'Deploying...'

