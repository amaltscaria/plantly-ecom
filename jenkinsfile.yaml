pipeline:
  agent:
    any
  stages:
    - name: 'Build'
      steps:
        - script:
            - npm install
            - npm test

    - name: 'Deploy'
      steps:
        - script:
            - npm run build
            - echo 'Deploying...'

